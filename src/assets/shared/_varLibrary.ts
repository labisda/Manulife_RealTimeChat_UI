interface options {
    url: string,
    showSuccessData?: boolean,
    data?: object,
    success?: Function,
    error?: Function,
    complete?: Function
};

const lib = {
    imgRoot: '../',
    imgIis: '' 
}

export default lib;